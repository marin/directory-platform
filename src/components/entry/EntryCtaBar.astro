---
import type { NormalizedEntry } from "../../lib/data/normalize-entry.ts";

interface Props {
  entry: NormalizedEntry;
}

const { entry } = Astro.props;
const hasActions =
  entry.isOpen &&
  (entry.bookingUrl || entry.website || entry.instagramUrl || entry.nap.formattedPhone);
---

{
  hasActions && (
    <div class="cta-bar" data-testid="entry-cta">
      {entry.bookingUrl && (
        <a
          class="cta cta-primary"
          href={entry.bookingUrl}
          rel="noopener noreferrer"
          data-testid="cta-booking"
        >
          Termin anfragen
        </a>
      )}
      {entry.nap.formattedPhone && (
        <a class="cta" href={`tel:${entry.nap.phone}`} data-testid="cta-phone">
          Anrufen: {entry.nap.formattedPhone}
        </a>
      )}
      {entry.website && (
        <a
          class="cta"
          href={entry.website}
          rel="noopener noreferrer"
          data-testid="cta-website"
        >
          Website öffnen
        </a>
      )}
      {entry.instagramUrl && (
        <a
          class="cta"
          href={entry.instagramUrl}
          rel="noopener noreferrer"
          data-testid="cta-instagram"
        >
          Instagram
        </a>
      )}
    </div>
  )
}
